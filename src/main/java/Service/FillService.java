package Service;

import Request.FillRequest;
import Result.FillResult;

/**
 * Used to attempt to fill a given amount of generations of the tree for the User specified in the Request
 */
public class FillService {
    /**
     * Attempts to perform the Fill Request
     *
     * @param r The Request to be performed
     * @return The Result of the attempt to perform the Request
     */
    public FillResult FillService(FillRequest r) {

        return null;
    }
}
