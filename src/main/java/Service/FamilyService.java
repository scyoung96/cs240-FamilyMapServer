package Service;

import Request.FamilyRequest;
import Result.FamilyResult;

/**
 * Used to attempt to find the entire tree of the current User
 */
public class FamilyService {
    /**
     * Attempts to perform the Family Request
     *
     * @param r The Request to be performed
     * @return The Result of the attempt to perform the Request
     */
    public FamilyResult FamilyService(FamilyRequest r) {

        return null;
    }
}
